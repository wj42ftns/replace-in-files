/* eslint-disable global-require */

describe('replace-in-files', () => {
  test('test work - simple string replace', () => {
    const replaceInFiles = require('../index.js');
    expect(5).toBe(5);
  });
});
